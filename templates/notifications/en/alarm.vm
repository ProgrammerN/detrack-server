#set($deviceLabel = "#{if}($device.model)$device.model#{else}$device.name#{end}")
#set($deviceLabelHtml = "#{if}($device.model)$esc.html($device.model)#{else}$esc.html($device.name)#{end}")
#set($eventTime = $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone))
#set($alarmKey = $event.getString('alarm'))
#set($alarmName = $translations.get("alarm${alarmKey.substring(0, 1).toUpperCase()}${alarmKey.substring(1)}"))
#set($subject = "$esc.html($deviceLabel): $esc.html($alarmName) alarm")
#set($digest = "$deviceLabel reported a $alarmName alarm at $eventTime")
<!DOCTYPE html>
<html>
<body>
Device: $deviceLabelHtml<br>
Alarm: $esc.html($alarmName)<br>
Time: $eventTime<br>
Point: <a href="$webUrl/event/$event.id">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Unsubscribe</a>
</body>
</html>
